#include <iostream>

int main ()
{

    int dividers{1};

    std::cout << "Input a number: ";
    int num {};
    std::cin >> num;


    for (int i = 1; i <= num/2; i++)
    {
        if (num % i == 0)
        {
            dividers++;
        }

    }

    if (dividers > 2)
    {
        std::cout << "The number isn't a prime.";
    } else
    {
        std::cout << "The number is a prime.";
    }

    return 0;
}
